# Server Configuration
PORT=5000
BASE_URL=http://localhost:5000
APP_STAGE=local

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_ADDRESS=your-email@example.com
SMTP_PASSWORD=your-password

# Authentication
APP_PASSWORD=your-secure-bearer-token

# Optional: Loki Logging (for centralized logging)
LOKI_HOST=http://localhost:3100
LOKI_USERNAME=admin
LOKI_BASIC_AUTH=password